import { Card } from '../../domain/decks/DeckEngine'

// Null cards (special placeholders / constrained cards); typically stable.
export const nullCards: Card[] = [
  // none by default — populated via handbook import when needed
]

export default nullCards
